# Netlify Functions Environment Variables
# Copy this file to .env.local for local development with `netlify dev`

# REQUIRED: LiteLLM API Key
# Get this from your LiteLLM deployment admin
LITELLM_API_KEY=your-api-key-here

# OPTIONAL: Override LiteLLM endpoint (default: https://app-litellmsn66ka.azurewebsites.net)
# LITELLM_ENDPOINT=https://your-litellm-endpoint.com

# OPTIONAL: Rate limiting configuration
# RATE_LIMIT_REQUESTS=60          # Max requests per window (default: 60)
# RATE_LIMIT_WINDOW_MS=60000      # Window duration in ms (default: 60000 = 1 minute)

# Frontend Configuration (Vite)
# API endpoint (Netlify function in production)
VITE_AI_ENDPOINT=/api/ai

# Available models (user can select in UI)
VITE_AI_DEFAULT_MODEL=google/gemini-2.0-flash
VITE_AI_MODELS=google/gemini-2.0-flash,azure/gpt-4o-mini

# Feature flags
VITE_FEATURE_AI_COPILOT=true
VITE_FEATURE_WEB_SEARCH=true

# For local development with direct LiteLLM access (bypasses Netlify function)
# VITE_DEV_LITELLM_API_KEY=your-key-for-local-dev
# VITE_DEV_LITELLM_ENDPOINT=https://app-litellmsn66ka.azurewebsites.net
